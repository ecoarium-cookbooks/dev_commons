# 
# /etc/hosts - generated by chef (cucumber::hosts recipe)
#

127.0.0.1 localhost.localdomain localhost
<% @servers.each do |server| %> 
<%= server['ip'] %> <%= server['fqdn'] %> <%= server['id'] %>
<% end %>
